<%= form_for(@english) do |f| %>
  <% if @english.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@english.errors.count, "error") %> prohibited this english from being saved:</h2>

      <ul>
      <% @english.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :meaning %><br>
    <%= f.text_area :meaning %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit Word' %>
  </div>
<% end %>
